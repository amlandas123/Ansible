# Installing nginx
- name: Installing nginx
  ansible.builtin.package:
    name: nginx
    state: present
#Starting nginx service
- name: starting nginx
  ansible.builtin.service:
    name: nginx 
    state: stopped
