- name: Demo on privilege escalation
  hosts: all
  become: true
  tasks:
    - name: Running Shell command
      ansible.builtin.shell: useradd -u 1006 tom ; cat /etc/passwd |grep tom
      register: user

    - name: Printing user details
      ansible.builtin.debug:
        msg: User {{user}} has been added

